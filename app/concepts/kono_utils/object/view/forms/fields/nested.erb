<% form.simple_fields_for(model) do |inside_form| %>

  <%= form.label model %>

  <% options[:fields].each do |f| %>
    <%= concept(base_class.concept_ns("cell/forms/field"), f, context: {form: inside_form}) %>
  <% end %>

<% end %>


