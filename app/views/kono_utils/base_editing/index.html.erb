<%= render 'index_page_title_header' %>
<%= render 'index_page_side_title_header' %>

<%= render 'search_form' unless @search.nil? %>

<%= concept(base_class.concept_ns("cell/index/table"), @objects, context: {current_user: current_user}) %>
