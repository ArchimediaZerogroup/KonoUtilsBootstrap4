<% if params.dig(:kono_utils, :target_container)

     container = nil
     simple_form_for(@object, as: "#{params.dig(:kono_utils, :structure_nested)}[#{Time.now.to_i}]", url: '#') do |form|
       container = concept(base_class.concept_ns("cell/forms/container"), @object,
                                                                context: {current_user: current_user, form: form})
     end

%>
  var content = window.atob("<%= Base64.strict_encode64(container.to_s) -%>");
  $("<%= params.dig(:kono_utils, :target_container) %>").append(content);
<% else %>
  console.warn("Devi passare in params kono_utils[target_container]=valore dove valore è il selettore css in cui APPENDERE il contenuto che generiamo per la new")
<% end %>